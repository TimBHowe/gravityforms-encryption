gravityforms-encryption
=======================
Allow the ability to enctyp text fields in the database, so sensitive data is only accesable by viewing the gravity form entry from the wordpress backend.
